import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'conexao.frontin',
  appName: 'conexao-frontin',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
